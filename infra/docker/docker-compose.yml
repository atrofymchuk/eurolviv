services:
  web:
    build:
      context: ../..
      dockerfile: infra/docker/Dockerfile
    expose:
      - "80"
    depends_on:
      wordpress:
        condition: service_started
    restart: unless-stopped

  wordpress:
    image: wordpress:6.7-php8.2-apache
    environment:
      WORDPRESS_DB_HOST: db
      WORDPRESS_DB_USER: ${WORDPRESS_DB_USER}
      WORDPRESS_DB_PASSWORD: ${WORDPRESS_DB_PASSWORD}
      WORDPRESS_DB_NAME: ${WORDPRESS_DB_NAME}
      WORDPRESS_CONFIG_EXTRA: |
        define('WP_HOME', '${WP_HOME_URL}');
        define('WP_SITEURL', '${WP_HOME_URL}');
        if (isset($$_SERVER['HTTP_X_FORWARDED_PREFIX']) && $$_SERVER['HTTP_X_FORWARDED_PREFIX'] !== '') {
          $$prefix = rtrim($$_SERVER['HTTP_X_FORWARDED_PREFIX'], '/');
          if (isset($$_SERVER['REQUEST_URI']) && strpos($$_SERVER['REQUEST_URI'], $$prefix) !== 0) {
            $$_SERVER['REQUEST_URI'] = $$prefix . $$_SERVER['REQUEST_URI'];
          }
        }
        if (isset($$_SERVER['HTTP_X_FORWARDED_PROTO']) && $$_SERVER['HTTP_X_FORWARDED_PROTO'] === 'https') {
          $$_SERVER['HTTPS'] = 'on';
        }
        define('EUROHOTEL_SITE_URL', '${SITE_URL:-https://eurohotel.pp.ua}');
    volumes:
      - wp_data:/var/www/html
      - ./wordpress/theme/eurohotel-blog:/var/www/html/wp-content/themes/eurohotel-blog:ro
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  db:
    image: mariadb:11
    environment:
      MYSQL_DATABASE: ${WORDPRESS_DB_NAME}
      MYSQL_USER: ${WORDPRESS_DB_USER}
      MYSQL_PASSWORD: ${WORDPRESS_DB_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    restart: unless-stopped

  wpcli:
    profiles: ["tools"]
    image: wordpress:cli-php8.2
    user: "33:33"
    environment:
      WORDPRESS_DB_HOST: db
      WORDPRESS_DB_USER: ${WORDPRESS_DB_USER}
      WORDPRESS_DB_PASSWORD: ${WORDPRESS_DB_PASSWORD}
      WORDPRESS_DB_NAME: ${WORDPRESS_DB_NAME}
    volumes:
      - wp_data:/var/www/html
      - ./wordpress/theme/eurohotel-blog:/var/www/html/wp-content/themes/eurohotel-blog:ro
    depends_on:
      db:
        condition: service_healthy

volumes:
  wp_data:
  db_data:
